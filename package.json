{
  "name": "nijjara-erp",
  "version": "1.0.0",
  "description": "Nijjara ERP Google Apps Script Project",
  "main": "Code.js",
  "scripts": {
    "pull": "clasp pull",
    "verify:appscript-paths": "node scripts/check-appscript-paths.js",
    "push": "npm run verify:appscript-paths && clasp push --force",
    "push:git": "git push origin main",
    "push:both": "npm run push && npm run push:git",
    "deploy": "npm run push:both",
    "save": "git add . && git commit -m 'WIP' && npm run deploy",
    "status": "git status && echo '\n--- Clasp Status ---' && clasp status || echo 'Clasp not logged in or configured'",
    "setup": "echo 'Setting up Nijjara ERP...' && npm install && echo '\nNext steps:' && echo '1. Run: clasp login (to authenticate with Google)' && echo '2. Run: clasp create --type standalone --title \"Nijjara ERP\" (if new project)' && echo '3. Run: clasp push (to push code to Apps Script)' && echo '4. Configure Git: git config credential.helper store' && echo '5. Run: npm run deploy (to sync everything)'",
    "deploy:web:new": "npx clasp version && npx clasp deploy -d \"Create new Web App deployment\" && npx clasp deployments",
    "deploy:web:redeploy": "node scripts/redeploy.js",
    "prepare": "husky install",
    "lint": "eslint . --ext .js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "health": "node scripts/health-check.js",
    "fix:paths": "node scripts/fix-paths.js",
    "nlp:parse": "node tools/nlp/cli.js",
    "request": "node scripts/pipeline.js",
    "test:browser": "node scripts/test-cli.js",
    "test:start": "node scripts/test-cli.js start",
    "test:deploy": "node scripts/test-cli.js deploy",
    "test:validate": "node scripts/test-cli.js validate",
    "test:status": "node scripts/test-cli.js status",
    "test:verify": "node scripts/browser-testing/verify-setup.js"
  },
  "keywords": [
    "apps-script",
    "erp",
    "google-apps-script"
  ],
  "author": "Nijjara ERP",
  "license": "ISC",
  "devDependencies": {
    "@google/clasp": "^3.1.1",
    "husky": "^9.1.0",
    "eslint": "^9.12.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "prettier": "^3.3.3",
    "@eslint/js": "^9.13.0",
    "globals": "^15.9.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
