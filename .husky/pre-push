#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

echo "Running pre-push safeguards..."
npm run verify:appscript-paths || {
  echo "Pre-push aborted due to policy violations."
  exit 1
}

echo "Pre-push checks passed. Proceeding."

